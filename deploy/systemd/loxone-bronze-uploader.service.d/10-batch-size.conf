# Defaults only: EnvironmentFile values override Environment entries.
# Update existing motherduck.env explicitly; see docs/uploader-operations.md.
[Service]
Environment=UPLOAD_BATCH_SIZE=5000
Environment=UPLOAD_MAX_BATCHES_PER_RUN=12
Environment=UPLOAD_MAX_RUNTIME_SECONDS=240
